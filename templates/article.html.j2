{% extends "base.html.j2" %}

{% block title %}{{ title }} - {{ brand_name }}{% endblock %}

{% block meta_tags %}
    <meta name="description" content="{{ summary[:160] }}">
    <link rel="canonical" href="{{ production_url }}/articles/{{ filename }}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{ production_url }}/articles/{{ filename }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ summary[:160] }}">
    {% if image_url %}<meta property="og:image" content="{{ image_url }}">{% endif %}
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="{{ production_url }}/articles/{{ filename }}">
    <meta name="twitter:title" content="{{ title }}">
    <meta name="twitter:description" content="{{ summary[:160] }}">
    {% if image_url %}<meta name="twitter:image" content="{{ image_url }}">{% endif %}
    <meta property="article:published_time" content="{{ iso_date }}">
{% endblock %}

{% block content %}
<article class="container mx-auto px-4 py-8">
    <header class="mb-6">
        <h1 class="text-3xl md:text-4xl font-bold text-gray-900">{{ title }}</h1>
        <p class="text-gray-500 text-sm">{{ date_human }}</p>
    </header>

    {% if image_url %}
    <div class="mb-8">
        <img src="{{ image_url }}" alt="{{ title }}" class="rounded-lg shadow-xl w-full h-auto object-cover">
    </div>
    {% endif %}

    <div class="prose prose-lg max-w-none">
        {{ summary | safe }}
    </div>

    <hr class="my-10">
    <aside class="text-sm text-gray-600">
        <div class="aurore-badge" style="display:inline-block;padding:2px 8px;border:1px solid #ddd;border-radius:9999px;margin-bottom:8px;">
            Généré par <strong>Aurore&nbsp;IA</strong>
        </div>
<<<<<<< HEAD
        <p>Verticale&nbsp;: <strong>{{ brand_name }}</strong> • <a href="{{ production_url }}">{{ production_url }}</a></p>
=======
        <p>Verticale : <strong>{{ brand_name }}</strong> • <a href="{{ production_url }}">{{ production_url }}</a></p>
>>>>>>> f1093225e097ed469ec0914a19a758b8892df8cd
    </aside>
</article>
{% endblock %}
