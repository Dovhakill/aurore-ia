{% extends "base.html.j2" %}

{% block title %}{{ title }} - {{ brand_name }} - Horizon Network{% endblock %}

{% block meta_tags %}
    <meta name="description" content="{{ summary[:160] }}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{ production_url }}/articles/{{ filename }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ summary[:160] }}">
    <meta property="og:image" content="{{ image_url }}">
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{ production_url }}/articles/{{ filename }}">
    <meta property="twitter:title" content="{{ title }}">
    <meta property="twitter:description" content="{{ summary[:160] }}">
    <meta property="twitter:image" content="{{ image_url }}">
{% endblock %}

{% block content %}
<article class="max-w-4xl mx-auto py-8 px-4">
    
    <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4 leading-tight">{{ title }}</h1>
    <p class="text-gray-500 text-sm mb-6">{{ date_human }}</p>

    {% if image_url and image_url != 'None' %}
    <div class="mb-8">
        <img src="{{ image_url }}" alt="{{ title }}" class="rounded-lg shadow-xl w-full h-auto object-cover">
    </div>
    {% endif %}

    <div class="prose lg:prose-xl max-w-none text-gray-800">
        <p>{{ summary | nl2br }}</p>
    </div>

    <hr class="my-12">

    <div class="bg-gray-100 border-l-4 p-4 rounded" style="border-color: {{ brand_color }};">
        <h4 class="font-bold text-lg">À propos de cet article</h4>
        <p class="text-sm text-gray-700 mt-2">
            Ce contenu a été synthétisé et généré par <strong>Aurore</strong>, une intelligence artificielle du réseau Horizon. Notre mission est de fournir une information factuelle et neutre, certifiée par un processus transparent et automatisé. 
            <a href="/a-propos.html" class="underline" style="color: {{ brand_color }};">En savoir plus sur notre démarche.</a>
        </p>
    </div>

</article>
{% endblock %}
