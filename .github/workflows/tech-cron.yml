name: Aurore Cron – Tech

on:
  workflow_dispatch:
  schedule:
    # Décalé de +30 min par rapport à Libre
    - cron: '30 2,6,10,14,18,22 * * *'

# Même groupe de concurrency pour éviter tout chevauchement
concurrency:
  group: aurore-global
  cancel-in-progress: false

jobs:
  run-tech:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install -U pip
          pip install -r requirements.txt

      - name: Run Aurore (Tech)
        env:
          GNEWS_API_KEY: ${{ secrets.GNEWS_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GH_TOKEN:       ${{ secrets.GH_TOKEN }}
          GITHUB_TOKEN:   ${{ secrets.GH_TOKEN }}     # Fallback pour le code
          BLOBS_PROXY_URL:     ${{ vars.BLOBS_PROXY_URL }}
          AURORE_BLOBS_TOKEN:  ${{ secrets.AURORE_BLOBS_TOKEN }}
          TWITTER_ACCESS_TOKEN:        ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
          TWITTER_API_KEY:             ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET_KEY:      ${{ secrets.TWITTER_API_SECRET_KEY }}
          TWITTER_BEARER_TOKEN:        ${{ secrets.TWITTER_BEARER_TOKEN }}
        run: |
          set -e
          python -m src.aurore --config tech
